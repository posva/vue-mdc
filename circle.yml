general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 4.2.2
  post:
    - npm i -g npm@3.x.x
test:
  override:
    - rm -rf dist || true
    - npm run build
    - rm -rf dist
    - npm test
