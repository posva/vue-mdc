machine:
  node:
    version: 4.2.x
  post:
    - npm i -g npm@3.x.x
dependencies:
  cache_directories:
    - bower_components
test:
  override:
    - rm -rf dist || true
    - npm run build
    - rm -rf dist
    - npm test
