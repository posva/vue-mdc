general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 4.2.2
  post:
    - npm i -g npm@3.x.x
test:
  override:
    - npm run build
    - npm test
deployment:
  doc:
    branch: dev
    commands:
      - ./build/gh-pages.sh
