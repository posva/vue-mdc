general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 4.2.2
  post:
    - npm i -g npm@3.x.x
test:
  override:
    - npm run build
    - git checkout dist
    - npm test
deployment:
  doc:
    branch: master
    commands:
      - ./build/gh-pages.sh
